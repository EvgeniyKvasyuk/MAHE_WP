import * as React from 'react';

import {
    Button,
} from './button';

describe('Button', () => {
    describe('basic render', () => {
        it('should render', () => {

        });
    });
});
